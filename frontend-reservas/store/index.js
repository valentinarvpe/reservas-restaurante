export const state = () => ({
    usuario: {}
})

export const actions = {
}

export const getters = {
    getUsuario(state) {
        state.usuario = res
        return res;
    }
}

export const mutations = {
    setUsuario(state, user ) {
        state.usuario = user
    }
}

